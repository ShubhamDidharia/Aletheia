fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
httpx>=0.27
pydantic>=2.9
langgraph>=1.0
langgraph-checkpoint>=2.0
google-genai>=1.0
redis>=5.0
websockets>=13.0

# Optional. Long-term memory (Commit 9) — no-ops without SUPABASE_* env vars.
supabase>=2.0
# Optional. Scraper fallback for pages Tavily can't read. Also needs a browser:
#   playwright install chromium
playwright>=1.40
